# JavaScript (Node.js) 语言运行配置 — 无编译期
# 环境: Ubuntu (GitHub Actions ubuntu-latest)，预装 node
# 语言标识: javascript（与 judge.sh 参考实现保持一致）

image: node:22-bookworm

# 无编译; 运行前语法检查: node --check main.js（可选）
compile: []

run:
  - node
  - main.js

# Node.js 运行时内存开销
memory_factor: 1.5

source_file: main.js
